# Core Flask - no compilation needed
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# Simple WebSocket alternative (no gevent dependency)
flask-sock==0.7.0

# Essential utilities - pure Python wheels
python-dotenv==1.0.0
requests==2.31.0
pillow==10.1.0

# AI
openai==1.3.8

# Browser automation - has pre-built wheels
playwright==1.40.0 